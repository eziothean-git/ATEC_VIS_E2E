# 课程学习 + 视觉探索混合策略

## 🎯 问题背景

### 原始课程学习的问题
- ✅ **优点**：机器人从简单地形开始，渐进式学习，训练稳定
- ❌ **缺点**：初期只在难度 0-1（平地/简单斜坡），**视觉多样性不足**
  - 视觉编码器可能过拟合到"平地"场景
  - FiLM 门控学不到处理复杂视觉场景的策略
  - 晋级到复杂地形时，视觉泛化能力差

## 💡 解决方案：混合策略

### 策略设计
保持课程学习的主体，同时引入**视觉探索环境**：

- **85% 环境**（约 870/1024）：课程学习
  - 在难度 0-1 训练，专注于学习运动策略
  - 稳定性优先，确保训练收敛
  
- **15% 环境**（约 154/1024）：视觉探索
  - 随机分布在所有难度（0-9）
  - 提供视觉多样性，避免视觉编码器过拟合
  - 这些环境可能失败，但不影响整体训练

### 核心思想
> "大部分环境专注于学习运动，少量环境专注于探索视觉场景"

这样既保持了课程学习的稳定性，又增强了视觉泛化能力。

## 📊 预期效果

### 初始难度分布
```
📚 难度 0:  435 个环境 (42.48%)  ← 课程学习
📚 难度 1:  435 个环境 (42.48%)  ← 课程学习
🎨 难度 2:   15 个环境 ( 1.46%)  ← 视觉探索
🎨 难度 3:   15 个环境 ( 1.46%)  ← 视觉探索
🎨 难度 4:   16 个环境 ( 1.56%)  ← 视觉探索
🎨 难度 5:   15 个环境 ( 1.46%)  ← 视觉探索
🎨 难度 6:   16 个环境 ( 1.56%)  ← 视觉探索
🎨 难度 7:   15 个环境 ( 1.46%)  ← 视觉探索
🎨 难度 8:   16 个环境 ( 1.56%)  ← 视觉探索
🎨 难度 9:   15 个环境 ( 1.46%)  ← 视觉探索
```

### 训练过程
1. **初期（0-200 iterations）**：
   - 课程学习环境稳定提升 reward
   - 视觉探索环境可能失败，但视觉编码器学习多样场景
   - FiLM 门控学习不同视觉→本体的调制策略

2. **中期（200-1000 iterations）**：
   - 课程学习环境逐步晋级到更高难度
   - 视觉编码器已经见过各种场景，泛化能力强
   - 视觉探索环境的 reward 也开始提升

3. **后期（1000+ iterations）**：
   - 所有环境收敛到高难度
   - 视觉泛化能力强，不会过拟合

## 🔧 配置参数

### sirius_curriculum_config.py
```python
class terrain(SiriusFlatCfg.terrain):
    # 课程学习基础参数
    max_init_terrain_level = 1  # 课程学习环境的最大难度
    
    # 视觉探索参数
    visual_exploration_ratio = 0.15  # 15% 环境用于视觉探索
    
    # 调整建议：
    # - 如果训练很稳定：可以增加到 0.20-0.25
    # - 如果训练不稳定：可以降低到 0.10 或 0.05
    # - 如果完全不需要：设为 0.0 即可
```

## 📈 优点总结

### ✅ 保留课程学习的优点
- 大部分环境仍然渐进式学习
- 训练稳定性不受影响
- Reward 曲线平滑上升

### ✅ 增强视觉泛化能力
- 视觉编码器从一开始就见到多种场景
- FiLM 门控学习处理复杂视觉场景
- 避免过拟合到平地场景

### ✅ 灵活可调
- 通过 `visual_exploration_ratio` 控制探索强度
- 可以在训练过程中动态调整
- 完全兼容原有课程学习逻辑

## 🎓 理论依据

这种策略类似于：
- **Exploration-Exploitation Trade-off**：大部分精力用于 exploit（课程学习），少量精力用于 explore（视觉探索）
- **Multi-task Learning**：同时学习运动策略和视觉泛化
- **Data Augmentation**：通过多样化的视觉输入增强泛化能力

## 🚀 使用方法

### 默认配置（推荐）
```bash
python train.py --task=sirius_curriculum --num_envs=1024
```

已自动启用 15% 视觉探索，无需修改代码。

### 调整探索比例
编辑 `sirius_curriculum_config.py`：
```python
visual_exploration_ratio = 0.20  # 增加到 20%
# 或
visual_exploration_ratio = 0.05  # 减少到 5%
# 或
visual_exploration_ratio = 0.0   # 完全禁用（纯课程学习）
```

### 监控训练
查看控制台输出：
```
============================================================
[Terrain Initialization] 地形课程学习配置
============================================================
  curriculum 启用: True
  max_init_terrain_level (配置): 1
  max_init_level (实际使用): 1
  num_rows (总难度数): 10
  num_cols (地形类型): 8
  num_envs (环境数): 1024
  visual_exploration_ratio: 15.0%
  课程学习环境: 870 (85.0%)
  视觉探索环境: 154 (15.0%)

初始地形难度分布:
  📚 难度 0:  435 个环境 (42.48%)
  📚 难度 1:  435 个环境 (42.48%)
  🎨 难度 2:   15 个环境 ( 1.46%)
  ...
============================================================
```

## 🔬 实验建议

### 对比实验
建议做 3 组对比实验：

1. **纯课程学习**（baseline）
   ```python
   visual_exploration_ratio = 0.0
   ```

2. **混合策略**（推荐）
   ```python
   visual_exploration_ratio = 0.15  # 默认
   ```

3. **激进探索**
   ```python
   visual_exploration_ratio = 0.30
   ```

对比指标：
- 训练稳定性（reward 曲线波动）
- 最终性能（最大 reward）
- 视觉泛化能力（在测试集上的表现）
- 晋级速度（多少次迭代达到高难度）

## ⚠️ 注意事项

1. **不影响课程学习主逻辑**
   - 视觉探索环境失败后，仍然会降级/重置
   - 不会"卡"在高难度无法完成

2. **适合长期训练**
   - 短期训练（< 500 iterations）可能看不出明显差异
   - 长期训练（1000+ iterations）视觉泛化优势明显

3. **硬件要求**
   - 15% 的探索环境不会显著增加计算量
   - 如果 GPU 内存紧张，可以适当减少探索比例

## 📚 参考

- 课程学习论文：[Curriculum Learning (Bengio et al., 2009)](https://ronan.collobert.com/pub/matos/2009_curriculum_icml.pdf)
- 探索-利用平衡：[Exploration vs Exploitation](https://en.wikipedia.org/wiki/Multi-armed_bandit)
- 视觉泛化：[Domain Randomization](https://arxiv.org/abs/1703.06907)

---

**✅ 已完成集成！无需额外配置，直接训练即可享受视觉探索的好处！**
